project('chibi-tech', 'c', 'cpp', 
    default_options : ['cpp_std=c++20'])

# Core engine functionality
subdir('ct-core')
subdir('ct-runtime')

# Plugins to the engine (optional features)

# Apps
subdir('ct-render-app') # render demo, does not require networking support
